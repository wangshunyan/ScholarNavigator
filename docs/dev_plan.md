# ScholarNavigator 开发主计划

这是仓库唯一的开发主计划。以当前工作树、测试和可读取的运行产物为准；`docs/contest/experiment-results.md` 只保存实验明细，不替代本计划。执行循环为：选择最高优先级的可执行项 → 实现 → 测试/评测 → 审查 → 更新本文件。

## 当前已核验基线（2026-08-25）

- 当前公共 `main` 已推送 GitHub；最新运行与 clean-clone 证据绑定提交 `1173973`。本地仅有 Git 忽略的实验资产变动。服务器原目录仍在 `eb7d151` 且有未提交内容，因此不在原目录直接启动新实验或覆盖文件；隔离服务器 worktree 跟踪当前公共 `main` 且干净（真实路径不进入发布包）。
- 本地 `outputs/server_assets/20260824_metadata_v3/` 的 v3 语料 SHA-256 为 `7a385c87250ff438f5748cc49ee683acf1edd01d2f12432d17fe60e83908a31a`，与服务器一致。它有 569,432 条唯一 arXiv ID；title、abstract、authors、year 完整，venue 12.152%、DOI 17.897%。该资产被 Git 忽略，仅作内部可审计实验输入。
- 同条件 200 条内部诊断显示 v3 Hybrid baseline 的 Recall@20 为 0.12850、F1@20 为 0.02108。Qwen3 Reranker 的 Recall@20 差值为 -0.00054（95% CI [-0.00987, 0.00838]）、F1@20 差值为 +0.00029（95% CI [-0.00219, 0.00273]）；不启用 Reranker。候选数 60→120 也无收益。上述均非官方比赛成绩。

## 共同约束

- 线上检索、索引、Prompt、connector 不得读取 gold/qrels；它们只能在检索结束后的离线评测中使用。
- 排序、召回、Reranker、Query Evolution、LLM Controller、全文引入必须做固定输入的成对实验；无显著正向收益不改默认策略。
- 不整体合并上游项目，只移植经测试证明确有价值且适配本架构的实现。
- 服务器实验使用隔离、干净的工作目录；只同步脱敏证据包、公开代码与结果摘要，不同步模型、索引、`.env`、私钥或原始运行目录。

## P0：可复现性与反馈闭环

- [x] **P0-01 当前提交的关键回归与前端构建**
  - 目标：以当前提交重新建立可读取的本地基线，而非依赖旧文档。
  - 验收：稳定基线提交 `373e5e2` 的全量后端回归为 `2348 passed, 184 skipped, 2 warnings`；当前 `1173973` 已通过 clean-clone smoke、5 条 gold-blind demo（5/5、每条至少 5 个可见结果、网络/LLM/gold-qrels 均为 0）、前端 lint/build 和 12 项全文安全回归。全量后端回归须在可控 CI/空闲执行环境重跑；此前在本机约 8% 时卡在既有长等待测试，不能伪称为当前全量通过。
  - 依赖：锁定的本地 `.venv` 与 `frontend/node_modules`。

- [x] **P0-02 隔离服务器实验工作区**
  - 目标：不改动服务器原有 dirty 工作树，创建基于当前公开提交的独立工作目录，并记录其 commit、依赖与 v3 资产校验。
  - 验收：2026-08-25 已创建独立服务器 worktree，运行代码在 `1173973` 已复核且当前 worktree 跟踪公共 `main`、保持干净；v3 语料 SHA-256、索引指纹 `91302a92…e68f71`、模型指纹 `926ed116…fdf1b7`、569,432 篇和 ANN Recall@10=0.984 均复核；新 worktree 专项回归 `23 passed`；原目录未修改。服务器使用受控的既有依赖运行时，隔离 worktree 不复制模型、索引或环境凭据。
  - 依赖：服务器磁盘空间、已授权 SSH、已存在的模型和 v3 索引。

- [x] **P0-03 可审计发布闭环复核**
  - 目标：确认队友从 GitHub 源码包可运行离线演示，且不依赖本机 `.env` 或私有数据。
  - 验收：2026-08-25 以当前 `1173973` 重跑 clean-clone smoke，返回 `ready`；发布包 36.98 MB、1,036 个文件，health/config=200，离线 BM25=5，网络/LLM/gold-qrels 均未加载，模板环境 API 搜索成功；发布 manifest 绑定该提交。
  - 依赖：P0-01 通过。

## P1：最终检索效果与证据质量

- [x] **P1-01 正式元数据契约与质量面板**
  - 目标：将 v3 的真实字段覆盖、来源、哈希和缺失字段公开到运行诊断/展示中；不填充或推断 venue、DOI。
  - 验收：2026-08-25 v3 核心字段门禁通过、全字段门禁如实失败；索引和运行时 API/界面均报告字段完整度、语料哈希、索引指纹（Hybrid 另含模型指纹）；相关后端测试 35 项及前端 lint/build 通过，内部指标边界保持明确。
  - 依赖：v3 语料、P0-02 的隔离服务器环境。

- [x] **P1-02 以检索覆盖和规则误杀为中心的改进实验**
  - 目标：依据已发现的“已召回但被规则误判”问题，设计最小、可解释的规则改进；不再盲目扩候选或启用 Reranker。
  - 验收：先做 30 条筛选；只有筛选达到预注册正向门槛才扩展到 200 条成对实验；所有候选策略必须在相同代码、语料、索引、查询、预算和资源约束下评估，且至少一项 Recall@20/F1@20 的 95% CI 下界大于 0 才可默认启用。若筛选未达门槛，则以可读取的负向证据关闭该策略，不盲目扩大实验。
  - 依赖：P0-02、P1-01；运行结果仅为内部指标。
  - 当前进展（2026-08-25）：已完成保守词形归一化的 30 条筛选。Recall@20 点估计 +0.0222，但 95% CI 为 [-0.0222, 0.0944]；F1@20 为 -0.00195，95% CI 为 [-0.0103, 0.0070]。两组均 30/30 成功，候选池和代码相同。未通过进入 200 条正式实验的预注册收益门槛，因此该策略的本轮资格实验闭环完成但默认保持 `current_rules`；词形归一化仅保留为 default-off 实验能力。脱敏证据已导入 `outputs/imported_server_evidence/`，成对分析为 `outputs/server_evidence/paired_lexical_probe_analysis.json`。另已复核 v3 Hybrid 的 200 条严格 Reranker-only 成对实验：严格审计通过（真实 CUDA、fallback=0），但 Recall@20 Δ -0.00054（95% CI [-0.00987, 0.00838]）、F1@20 Δ +0.00029（95% CI [-0.00219, 0.00273]），不满足启用门槛；Qwen3 Reranker 保持 default-off。

- [ ] **P1-03 低质量论文过滤（保守、可解释）**
  - 目标：把撤稿和重复风险的独立证据接入现有质量报告；未知状态不扣分，质量分与相关性分分离。
  - 验收：只在可验证的外部证据明确标记时显示/软影响结果；API、前端和测试同步；对固定样本验证排序变化并评估 Recall/F1 影响。
  - 依赖：可公开访问的撤稿/更正数据源与 P1-01 的稳定身份字段。
  - 当前进展（2026-08-25）：Crossref 撤稿关系采集、严格 ledger 绑定、质量信号 API/前端展示及 fail-closed 测试已存在。补齐了 Crossref 明确 `relation.retraction` 的识别（原先只识别 `is-retracted-by`/`update-to`），并以公开撤稿 DOI `10.1007/s11613-016-0476-y` 真实 smoke 生成了 `flagged` ledger；未知状态仍不扣分。历史 20 条 arXiv→Crossref probe 未发现实际候选中的明确证据（7 条无 DOI、10 条未返回、3 条无明确关系）；随后用 1,362 个 Crossref 明确撤稿 DOI 与 v3 语料精确 DOI 交集，命中为 0。v3 DOI 覆盖只有 17.897%，因此尚不能声称“过滤”带来 Recall/F1 收益；质量 soft 保持默认关闭。
  - **BLOCKED（外部数据）**：需要一个固定真实候选集，其中至少有一篇带稳定 DOI/arXiv 身份、可公开复核的明确撤稿/更正证据；取得后才可执行质量 soft-ranking 的严格成对实验。不得以外部临时论文替换当前 v3 候选来伪造收益。

- [ ] **P1-04 全文证据展示与定位**
  - 目标：将已验证许可的全文段落稳定呈现给用户，并保持来源、许可证、段落定位和原文哈希可追溯。
  - 验收：真实允许来源的 HTML/PDF 至少完成一个端到端 smoke；无许可、超限、解析失败均 fail-closed；全文不改变默认排序，Evidence F1 需单独评测。
  - 依赖：允许抓取的开放全文与 P0-02。
  - 当前进展（2026-08-25）：新增独立 `POST /api/v1/full-text/fetch` 受控入口，要求调用方提供已核验许可证和精确 HTTPS 主机 allow-list；全文不进入搜索排序。新增 XML/HTML 解析和反爬挑战页拒绝逻辑，后端全文/API 专项回归 39 项通过。前端论文卡片已增加默认折叠的“按许可获取全文证据”控件：仅使用该结果已绑定的 HTTPS PDF 地址，要求许可证标识和人工核验确认；任意 URL 只能经受控数据导入流程进入，不能在卡片粘贴。成功后展示段落、字符范围和哈希，不修改排名；前端 lint/build 均通过。新增 `evidence-f1-v1` 段落级离线评测器和输入契约，缺少人工 gold 时明确返回 `pending_human_labels`，不输出伪造零分。服务器与本地 Europe PMC `fullTextXML` smoke 均成功（CC0、application/xml、2 个可定位段落，内容哈希 `018532f2…2d7f`）；PMC HTML challenge 被识别并 fail-closed。真实查询集合的人工 Evidence F1 与自动全文接入仍未完成，因此本项保持未完成。
  - **BLOCKED（人工标注）**：需要独立标注者针对固定真实查询-paper 集合，提供已核验全文的段落级证据标签；收到后可直接用 `evidence-f1-v1` 计算，不应由模型或系统推断替代人工标签。

## P2：受控智能增强与参赛交付

- [ ] **P2-01 LLM 搜索增强的 Record/Replay 实验**
  - 目标：保持 LLM 默认关闭，补齐可观察动作、Schema、预算、Record/Replay 与规则回退。
  - 验收：无 Provider 时稳定降级；有 Provider 时完成同条件 200 条 paired experiment，只有严格正向结果才开放默认开关。
  - 依赖：P1-02 通过、可用 Provider、固定模型/Prompt 快照。
  - **BLOCKED（Provider）**：当前没有经用户授权、可审计的 LLM Provider/模型快照；保持默认关闭，不能用未固定的第三方模型结果声称改进。

- [x] **P2-02 竞赛发布包与 5 分钟演示**
  - 目标：输出可复现的源码发布包、零凭据离线演示和一组不依赖 gold 的评委查询。
  - 验收：P0-03 通过；演示脚本可完整走通查询理解、检索、质量说明、证据定位和导出；不声称内部指标是官方成绩。
  - 依赖：P0-03、P1-04。
  - 当前进展（2026-08-25）：运行证据提交 `1173973` 已重跑 5 条 gold-blind demo，5/5 成功、每条可见结果不少于 5、网络/LLM/gold-qrels 均为 0；clean-clone 发布包亦返回 `ready`。演示将全文证据明确标为按许可、可选且 fail-closed 的能力；P1-04 的独立 Evidence F1 缺失不冒充为演示成绩。

- [ ] **P2-03 参赛材料一致性审查**
  - 目标：技术报告、项目说明、实验表、限制声明与仓库实际能力一致。
  - 验收：逐项核对提交规范；所有数字有可读取证据；团队/官方赛制信息由用户补全后生成最终包。
  - 依赖：P1/P2 实验与官方提交材料。
  - **BLOCKED（官方材料）**：需要团队信息、官方提交模板/截止要求和官方 scorer 或评分规则；在这些输入缺失时只能维护真实能力说明，不能生成可提交的最终参赛包。

## 外部阻塞

- **官方 scorer、正式人工标注与团队信息**：尚未提供，不能把内部 200 条诊断升级为官方成绩。
- **服务器原目录存在未提交内容**：通过 P0-02 的隔离工作区绕开，不覆盖用户原实验。
